# flunky.beer

```bash
# start the client, server and prisma studio
$ pnpm dev

# start the client
$ pnpm client
# start the server
$ pnpm server
# start prisma studio
$ pnpm studio

# generate the prisma client
$ pnpm generate

# reset the database, apply migrations and seed it with data
$ pnpm reset
```
